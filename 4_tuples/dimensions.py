dimensions = (200, 5)   # dimensions for a rectangular which is constant throughout the program

print(f"Length of the Rectangle : {dimensions[0]}")
print(f"Breadth of the Rectangle : {dimensions[1]}")

# dimensions_circle = (10)  will cause error, the correct syntax is:

dimensions_circle = (10,)

print(f"Radius of circle = {dimensions_circle[0]}")